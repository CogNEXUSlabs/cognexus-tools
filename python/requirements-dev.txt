# Development / integration-test extras for artzain (pypi-package).
# Install: pip install -r requirements-dev.txt

pytest>=8.0
ruff>=0.4
build
twine
pyyaml>=6.0

# Optional: examples/protected_inference.py and test_optional_model_smoke_inference
# (the Gemma 4 demo model needs transformers >= 5.5)
torch>=2.0
transformers>=5.5
accelerate>=0.27
